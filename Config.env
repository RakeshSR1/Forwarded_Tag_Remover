import os

# ----------------------- BOT SETTINGS -----------------------
API_ID = int(os.getenv("API_ID", "12345678"))  # Your Telegram API ID
API_HASH = os.getenv("API_HASH", "your_api_hash")  # Your Telegram API Hash
BOT_TOKEN = os.getenv("BOT_TOKEN", "your_bot_token")  # Telegram Bot Token

OWNER_ID = int(os.getenv("OWNER_ID", "123456789"))  # Your Telegram ID
AUTH_USERS = set(map(int, os.getenv("AUTH_USERS", "123456789").split()))  # Multiple IDs separated by space

# ----------------------- STORAGE SETTINGS -----------------------
DOWNLOAD_DIR = os.getenv("DOWNLOAD_DIR", "downloads")  # Folder for downloads
MAX_FILE_SIZE = int(os.getenv("MAX_FILE_SIZE", "4000000000"))  # 4GB for Telegram

# ----------------------- GDRIVE SETTINGS -----------------------
USE_GDRIVE = os.getenv("USE_GDRIVE", "True").lower() == "true"
GDRIVE_FOLDER_ID = os.getenv("GDRIVE_FOLDER_ID", "")  # Google Drive folder ID
GDRIVE_CREDENTIALS = os.getenv("GDRIVE_CREDENTIALS", "credentials.json")  # OAuth JSON

# ----------------------- PROGRESS BAR -----------------------
PROGRESS_UPDATE_INTERVAL = int(os.getenv("PROGRESS_UPDATE_INTERVAL", "5"))  # Seconds between updates

# ----------------------- ZIP SETTINGS -----------------------
AUTO_ZIP = os.getenv("AUTO_ZIP", "False").lower() == "true"
ZIP_PASSWORD = os.getenv("ZIP_PASSWORD", "")  # Optional password

# ----------------------- LOGGING -----------------------
LOG_CHANNEL = int(os.getenv("LOG_CHANNEL", "0"))  # Channel for logs, 0 = off
DEBUG = os.getenv("DEBUG", "False").lower() == "true"

# ----------------------- OTHER -----------------------
FFMPEG_PATH = os.getenv("FFMPEG_PATH", "/usr/bin/ffmpeg")  # Path to ffmpeg

#------------------------ Commends ----------------------

/start              → Show welcome/help message
/compress           → Compress a media file
/zip                → ZIP multiple files
/unzip              → Unzip a given ZIP/RAR file
/vcompress          → Compress a video to lower size/quality
/progress           → Show current progress of ongoing task
/cancel             → Cancel ongoing process
